/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PercentileGraphComponent } from './percentile-graph.component';

describe('Component: PercentileGraph', () => {
  it('should create an instance', () => {
    let component = new PercentileGraphComponent();
    expect(component).toBeTruthy();
  });
});
